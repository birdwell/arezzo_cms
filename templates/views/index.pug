extends ../layouts/default

block content
	.container: .jumbotron
		h1 Welcome to Arezzo CMS
		p This site populates the Arezzo Mobile Application.
		p The following types can be created.
		ul 
			li Events
			li Food
			li Outdoors
			li Shopping
			li Sights
		hr
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Open the Admin UI
		else
			p The default user has the email <strong>user@ou.edu</strong> and the password <strong>temp001</strong>.
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.
